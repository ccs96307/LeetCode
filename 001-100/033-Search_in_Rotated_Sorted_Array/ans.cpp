class Solution {
public:
    int search(vector<int>& nums, int target) {        
        int first = nums[0];
        int i = nums.size() / 2;
        
        while (true) {
            if (first > )
        }
        for (int i=0; i<nums.size(); ++i) {
            if (nums[i] == target) return i;
        }
        
        return -1;
    }
};

